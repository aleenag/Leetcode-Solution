class Solution {
    public int maximizeSum(int[] nums, int k) {
        int max = 0;
        for(int i : nums){
            if(i > max){
                max = i;
            }
        }
    return k*(max + (max+k-1))/2;
        
    }
}
